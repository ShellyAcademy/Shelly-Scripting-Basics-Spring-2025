let arr = [4, 5, 6, 7, 8, 9, 7];

// returns the index of first occurance
console.log(arr.indexOf(7));

// when not found, it will return -1
console.log(arr.indexOf(10));

console.log(arr.indexOf(7, 4));

// Not supported by Shelly JS
// arr.reverse();
// console.log(arr);